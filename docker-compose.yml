services:
  redis:
    image: redis:7-alpine
    container_name: fitmarket-redis
    ports:
      - "6379:6379"
    command: redis-server --maxmemory 256mb --maxmemory-policy volatile-ttl
    volumes:
      - redis-data:/data
    restart: unless-stopped

volumes:
  redis-data:
